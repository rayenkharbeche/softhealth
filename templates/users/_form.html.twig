
{% block titre %}

{% endblock %}
{% block body %}
<body>

<div class="container">
    <div class="form">

 {{ form_start(form,{
     'attr': {
         'novalidate': 'novalidate',

     }
 }

 ) }}

<div class="form-group">



    {{ form_row(form.email) }}
    {{ form_errors(form.email) }}
</div>

<div class="form-group"  >

    {{ form_row(form.roles) }}

</div>

<div  class="form-group">
    {{ form_row(form.password) }}


</div>

<div class="form-group">
    {{ form_row(form.cin) }}
    {{ form_errors(form.cin) }}
</div>

<div>
    {{ form_row(form.username) }}
    {{ form_errors(form.username) }}
</div>



<div class="form-group">

    <button class="btn btn-lg btn-primary">{{ button_label|default('Ajouter') }}</button>
</div>

<div class="alert alert-info">{{form_rest(form) }} </div>

        {{ form_end(form) }}

        

    </div>
</div>
</body>
{% endblock %}

